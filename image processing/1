import numpy as np
import cv2
x=0
y=0
img = cv2.imread("BCB.png")
Mat imgarray[2][4]
for i in range (1):
    for j in range (3):
        imgarray[i][j] = img[x:x+200, y:y+200]
        x=x+200
    y=y+200
cv2.imshow('last', imgarray[1][3])
cv2.waitKey(0)
cv2.destroyAllWindows()
